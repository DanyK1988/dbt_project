seeds:
  - name: city_region
    config:
      column_types:
        city: varchar(50)
        region: varchar(50)
  - name: passanger_id
    config:
      column_types:
        passenger_id: varchar(20)
        passenger_name: varchar(50)
